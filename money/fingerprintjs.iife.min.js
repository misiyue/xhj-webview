/* FingerprintJS vendored script placeholder.
 * The local bridge falls back to browser-persisted UUID when FingerprintJS
 * is unavailable, so H5 registration still produces a stable device_code.
 */
